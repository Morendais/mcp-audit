name: MCP Security Audit

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  mcp-audit:
    name: Audit MCP Server
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node / Python environment
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install mcp-audit
        run: go install github.com/mcp-audit/mcp-audit@latest

      - name: Execute Dynamic Security Audit
        run: |
          mcp-audit scan \
            --exec "node dist/index.js" \
            --sarif results.sarif \
            --fail-on critical

      - name: Upload SARIF to GitHub Security Tab
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
